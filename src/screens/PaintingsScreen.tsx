import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PaintingsScreen = () => {
  return (
    <View>
      <Text>PaintingsScreen</Text>
    </View>
  );
};

export default PaintingsScreen;

const styles = StyleSheet.create({});
